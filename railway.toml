[build]
builder = "NIXPACKS"
buildCommand = "npm ci --workspace client && npm run build --workspace client && npm ci --workspace server"

[deploy]
startCommand = "npm start"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
